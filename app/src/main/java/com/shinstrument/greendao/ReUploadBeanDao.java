package com.shinstrument.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.shinstrument.Bean.DataFlow.ReUploadBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RE_UPLOAD_BEAN".
*/
public class ReUploadBeanDao extends AbstractDao<ReUploadBean, Long> {

    public static final String TABLENAME = "RE_UPLOAD_BEAN";

    /**
     * Properties of entity ReUploadBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Method = new Property(1, String.class, "method", false, "METHOD");
        public final static Property Content = new Property(2, byte[].class, "content", false, "CONTENT");
        public final static Property Type_patrol = new Property(3, int.class, "type_patrol", false, "TYPE_PATROL");
    }


    public ReUploadBeanDao(DaoConfig config) {
        super(config);
    }
    
    public ReUploadBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RE_UPLOAD_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"METHOD\" TEXT," + // 1: method
                "\"CONTENT\" BLOB," + // 2: content
                "\"TYPE_PATROL\" INTEGER NOT NULL );"); // 3: type_patrol
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RE_UPLOAD_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ReUploadBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String method = entity.getMethod();
        if (method != null) {
            stmt.bindString(2, method);
        }
 
        byte[] content = entity.getContent();
        if (content != null) {
            stmt.bindBlob(3, content);
        }
        stmt.bindLong(4, entity.getType_patrol());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ReUploadBean entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String method = entity.getMethod();
        if (method != null) {
            stmt.bindString(2, method);
        }
 
        byte[] content = entity.getContent();
        if (content != null) {
            stmt.bindBlob(3, content);
        }
        stmt.bindLong(4, entity.getType_patrol());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ReUploadBean readEntity(Cursor cursor, int offset) {
        ReUploadBean entity = new ReUploadBean( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // method
            cursor.isNull(offset + 2) ? null : cursor.getBlob(offset + 2), // content
            cursor.getInt(offset + 3) // type_patrol
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ReUploadBean entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMethod(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setContent(cursor.isNull(offset + 2) ? null : cursor.getBlob(offset + 2));
        entity.setType_patrol(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ReUploadBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ReUploadBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ReUploadBean entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
